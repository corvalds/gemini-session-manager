# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Gemini Session Manager — a Chrome extension (Manifest V3) for managing Gemini sessions.

## Structure

- `manifest.json` — Chrome extension manifest (MV3)

## Development

This is a vanilla Chrome extension project. Load it in Chrome via `chrome://extensions/` with "Developer mode" enabled, using "Load unpacked" pointed at the project root (or `dist/` if a build step is added later).
